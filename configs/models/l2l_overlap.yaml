experiment:
  name: poisson_l2l_overlap_seed0
  output_dir: results/poisson_l2l_overlap_seed0

pca:
  type: local_to_local

patches:
  patch_size: 64
  stride: 32
  assembly: hann_safe

model:
  type: mlp
