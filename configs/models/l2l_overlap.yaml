experiment:
  name: poisson_l2l_overlap_seed0
  output_dir: results/poisson_l2l_overlap_seed0

pca:
  type: local_to_local

patches:
  patch_size: 32
  stride: 16
  assembly: hann_safe

model:
  type: mlp
