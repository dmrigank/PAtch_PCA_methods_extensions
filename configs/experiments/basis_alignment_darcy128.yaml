experiment:
  name: basis_alignment_darcy128
  seed: 0
  output_dir: results/basis_alignment

mechanisms:
  two_scale: false
  coupling: false
  in_loop_loss: false
  boundary_correction: false

pca:
  guard_band: 0
  basis_alignment: true

patches:
  patch_size: 32
  stride: 32
  assembly: average

training:
  device: cuda
